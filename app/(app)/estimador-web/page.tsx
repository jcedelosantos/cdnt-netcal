import EstimadorWebClient from './_components/estimador-web-client';

export const metadata = { title: 'Estimador web' };

export default function EstimadorWebPage() {
  return <EstimadorWebClient />;
}
